import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: ' Address to taking coordinates and showing map with Path from current location',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
